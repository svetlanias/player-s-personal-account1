spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/CS
    username: postgres
    password: Sofa44@))$cjy
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:

logging:
  level:
    org.flywaydb: DEBUG

server:
  port: 8084